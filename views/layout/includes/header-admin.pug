header.bg-amber-600.p-4
    .container.mx-auto.flex.justify-between.items-center
        a(href="/") 
            img(class="h-16 mx-auto" src="/img/logo.png" alt="")

        nav(class="my-5 text-sm md:flex md:items-center md:gap-3 font-bold text-white hidden")
            a(href="/mis-emprendimientos") Mis emprendimientos
            a(href="") Mi perfil

            form(action="/auth/cerrar-sesion" method="POST")
                input(type="hidden", name="_csrf" value= csrfToken)
                input(type="submit" class="bg-amber-800 py-2 px-10 rounded-lg" value="Cerrar Sesión") 